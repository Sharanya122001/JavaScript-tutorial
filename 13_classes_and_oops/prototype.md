JavaScript is NOT class-based like C# or Java.
It is Prototype-based.

That means:
Objects inherit from other objects.
Not from classes (internally).

🔹 Simple Meaning
Prototype =
👉 An object that another object can inherit properties from.

#######READ ABOUT PROTOTYPE